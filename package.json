{
  "name": "cli-app",
  "version": "1.0.0",
  "description": "testing node cli input",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,json,css,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "author": "Phill arron",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "prompt": "^1.0.0"
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "lint-staged": "^7.3.0",
    "prettier": "1.14.2"
  }
}
